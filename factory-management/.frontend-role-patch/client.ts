import{TokenManager}from'../utils/TokenManager';const BASE_URL=import.meta.env.VITE_API_URL??'http://localhost:8080/factory-management/api/v1';type Envelope<T>={code:number;message?:string;result:T};async function request<T>(path:string,method='GET',data?:unknown,auth=true):Promise<T>{const token=TokenManager.token();const r=await fetch(`${BASE_URL}${path}`,{method,headers:{'Content-Type':'application/json',...(auth&&token?{Authorization:`Bearer ${token}`}:{})},body:data===undefined?undefined:JSON.stringify(data)});let b:Envelope<T>;try{b=await r.json()}catch{throw new Error('Máy chủ trả về dữ liệu không hợp lệ')}if(r.status===401&&auth){TokenManager.clear();window.dispatchEvent(new Event('factory:unauthorized'))}if(!r.ok||b.code!==1000)throw new Error(b.message??'Không thể thực hiện yêu cầu');return b.result}export const get=<T>(path:string)=>request<T>(path);export const send=<T>(path:string,method:'POST'|'PUT'|'DELETE',data?:unknown)=>request<T>(path,method,data);export const publicPost=<T>(path:string,data:unknown)=>request<T>(path,'POST',data,false)
