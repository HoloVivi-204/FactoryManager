import{get,send}from'./client';export type Portal={employeeCode:string;employeeName:string;position:string;teamName:string;workingMinutes:number;overtimeMinutes:number;unreadNotifications:number;schedules:any[];attendance:any[];kpis:any[];leaves:any[];notifications:any[]};export const employeeApi={dashboard:()=>get<Portal>('/employee-portal/dashboard'),leave:(data:unknown)=>send('/employee-portal/leave-requests','POST',data),read:(id:number)=>send(`/employee-portal/notifications/${id}/read`,'PUT')}
