import{useEffect,useState}from'react';import{useAuth}from'./context/AuthContext';import DashboardLayout from'./layouts/DashboardLayout';import AppRoutes from'./routes/AppRoutes';import LoginPage from'./pages/LoginPage';import type{PageKey}from'./types';export default function App(){const{user,loading,hasRole}=useAuth();const[page,setPage]=useState<PageKey>('overview');useEffect(()=>{if(user&&hasRole('ADMIN'))setPage('admin-master')},[user]);if(loading)return <div className="app-loading">Đang xác thực phiên đăng nhập…</div>;if(!user)return <LoginPage/>;return <DashboardLayout page={page} onNavigate={setPage}><AppRoutes page={page}/></DashboardLayout>}
